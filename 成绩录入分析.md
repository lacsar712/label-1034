# 成绩录入防重复、校验失败返回及鉴权风险分析

## 一、新增成绩时如何防止同一学生对同一课程重复录入

仓库中通过 **应用层查询校验** 和 **数据库层唯一约束** 两层机制来防止重复录入。

### 1. 应用层：创建接口中的查询校验

在 `backend/app/api/grades.py:79-99` 的 `create_grade` 函数中，新增成绩前会先查询是否已存在同一学生同一课程的成绩记录：

```python
r = await db.execute(
    select(Grade).where(
        Grade.student_id == data.student_id, Grade.course_id == data.course_id
    )
)
if r.scalar_one_or_none():
    raise HTTPException(status_code=400, detail="该学生该课程已有成绩记录")
```

- 先以 `student_id` 和 `course_id` 为条件查询 `grades` 表；
- 若查到记录，直接抛出 `HTTPException`，阻止插入。

### 2. 数据库层：唯一约束

在 `backend/app/models/grade.py:10` 的 `Grade` 模型中，定义了联合唯一约束：

```python
__table_args__ = (UniqueConstraint("student_id", "course_id", name="uq_student_course"),)
```

- 数据库表 `grades` 上存在名为 `uq_student_course` 的唯一索引，确保 `(student_id, course_id)` 组合在数据库层面唯一；
- 即使应用层校验因并发请求而未拦截，数据库层的唯一约束仍会阻止重复写入（此时会触发 `IntegrityError`，由 `database.py:36-39` 的异常处理回滚事务）。

**两层机制互为补充**：应用层给出友好的错误提示，数据库层是最终的数据一致性保障。

---

## 二、校验失败时接口返回什么

所有校验失败均通过 `HTTPException` 返回，FastAPI 会将其序列化为标准 JSON 错误响应，格式为：

```json
{
  "detail": "<错误描述>"
}
```

具体校验场景及返回如下（均在 `backend/app/api/grades.py` 中）：

| 校验场景 | 文件位置 | HTTP 状态码 | detail 内容 |
|---|---|---|---|
| 学生不存在 | `grades.py:82-83` | 400 | `"学生不存在"` |
| 课程不存在 | `grades.py:85-86` | 400 | `"课程不存在"` |
| 同一学生同一课程已有成绩 | `grades.py:92-93` | 400 | `"该学生该课程已有成绩记录"` |
| 成绩记录不存在（查询/更新/删除时） | `grades.py:64`, `grades.py:108`, `grades.py:120` | 404 | `"成绩记录不存在"` |

此外，Pydantic Schema 层（`backend/app/schemas/grade.py:10`）定义了 `score` 字段的范围约束：

```python
score: Decimal = Field(..., ge=0, le=100)
```

当 `score` 不在 `[0, 100]` 范围内时，FastAPI 会在请求体解析阶段自动返回 **422 Unprocessable Entity**，响应体格式为：

```json
{
  "detail": [
    {
      "loc": ["body", "score"],
      "msg": "<具体校验失败信息>",
      "type": "<校验错误类型>"
    }
  ]
}
```

---

## 三、接口未做登录鉴权可能带来的风险

### 当前状态

经搜索仓库全部代码，**未找到任何登录鉴权相关实现**：

- 无 auth/login/token/jwt 相关模块或中间件；
- `backend/app/main.py` 中仅配置了 `CORSMiddleware`（允许所有来源 `*`），未添加任何鉴权中间件；
- 所有 API 路由（students、courses、grades、classes）均无 `Depends(...)` 形式的鉴权依赖注入；
- 无用户模型（User model）、无用户表。

### 可能带来的风险

1. **任何人可无限制访问和操作数据**：所有 CRUD 接口完全公开，任何人无需身份认证即可查询、新增、修改、删除学生、课程、成绩等所有数据。

2. **无法区分操作权限**：没有角色概念（如管理员、教师、学生），无法实现"只读"或"仅可操作自己数据"等权限控制。

3. **数据被恶意篡改或删除的风险**：攻击者可直接调用 DELETE/PUT 接口批量篡改或删除成绩、学生等信息，且无法追溯操作者。

4. **无法做操作审计**：代码中未找到任何操作日志记录（除简单的 `logger.info` 外无用户身份关联），无法追踪"谁在何时做了什么"。

5. **CORS 配置过于宽松**：`allow_origins=["*"]` 配合无鉴权，意味着任意域名的前端页面都可以直接调用后端 API，增加了 CSRF 攻击的风险。

---

*本分析仅基于仓库内已有代码，未编造任何未出现的模块或接口。*
