# 成绩管理系统接口分析

## 一、新增成绩时如何防止同一学生对同一课程重复录入

系统通过**数据库唯一约束**与**应用层前置查询**双重机制防止重复录入：

### 1. 数据库唯一约束（根本保障）

在模型层定义了 `(student_id, course_id)` 联合唯一约束，从数据库层面禁止重复数据插入：

**文件**：`backend/app/models/grade.py:10`
```python
__table_args__ = (UniqueConstraint("student_id", "course_id", name="uq_student_course"),)
```

### 2. 应用层前置查询（快速失败）

在创建成绩接口中，插入数据前先查询是否已存在同学生同课程的成绩记录，若存在则直接抛出异常：

**文件**：`backend/app/api/grades.py:87-93`
```python
r = await db.execute(
    select(Grade).where(
        Grade.student_id == data.student_id, Grade.course_id == data.course_id
    )
)
if r.scalar_one_or_none():
    raise HTTPException(status_code=400, detail="该学生该课程已有成绩记录")
```

---

## 二、校验失败时接口返回什么

接口通过 FastAPI 的 `HTTPException` 统一返回 JSON 格式的错误响应，包含 HTTP 状态码与 `detail` 错误描述字段。

### 返回格式
```json
{
    "detail": "错误描述信息"
}
```

### 各类校验失败场景对应的返回

| 场景 | HTTP 状态码 | detail 字段内容 | 位置 |
|------|------------|----------------|------|
| 学生不存在 | 400 | "学生不存在" | `backend/app/api/grades.py:83` |
| 课程不存在 | 400 | "课程不存在" | `backend/app/api/grades.py:86` |
| 该学生该课程已有成绩记录 | 400 | "该学生该课程已有成绩记录" | `backend/app/api/grades.py:93` |
| 成绩记录不存在（查询/更新/删除时） | 404 | "成绩记录不存在" | `backend/app/api/grades.py:64,108,120` |
| 学号重复 | 400 | "学号已存在" | `backend/app/api/students.py:59,82` |
| 班级不存在 | 400 | "班级不存在" | `backend/app/api/students.py:56,78` |

此外，Pydantic Schema 层还会做基础校验：
- `score` 字段范围限制 `0 <= score <= 100`（`backend/app/schemas/grade.py:10`），若超出将返回 422 状态码与校验错误详情。

---

## 三、当前接口未做登录鉴权可能带来的风险

### 代码中未找到
代码中未找到用户表、认证/鉴权依赖（如 `Depends` 形式的认证注入）、Token 校验中间件、或任何用户身份验证相关的业务逻辑。

### 潜在风险

1. **未授权访问**：任意客户端可直接调用所有接口，读取、修改或删除学生、课程、成绩数据，缺乏访问控制。

2. **数据篡改风险**：任何人都可以新增、更新或删除成绩记录，恶意用户可篡改他人成绩或录入虚假数据。

3. **无法审计追责**：所有写操作（创建、更新、删除）的日志中仅记录了 ID 等业务数据（`backend/app/api/grades.py:98,112,123`），无法追溯到具体操作人，出现问题无法排查。

4. **数据泄露风险**：所有成绩数据可被任意查询获取，学生个人成绩属于敏感信息，缺乏权限控制会导致数据泄露。

5. **接口滥用风险**：缺乏速率限制与身份标识，接口可能被恶意调用进行批量数据爬取或服务攻击。
