# 成绩录入校验及鉴权分析

## 一、防止同一学生对同一课程重复录入的实现

系统通过**双层校验**机制防止重复录入：

### 1. 数据库层唯一约束

在 `backend/app/models/grade.py:10` 中定义了数据库表级唯一约束：

```python
__table_args__ = (UniqueConstraint("student_id", "course_id", name="uq_student_course"),)
```

该约束确保 `grades` 表中 `student_id` 和 `course_id` 的组合唯一，从数据库层面杜绝重复记录。

### 2. 业务逻辑层前置校验

在 `backend/app/api/grades.py:87-93` 的 `create_grade` 接口中，写入数据库前先执行查询校验：

```python
r = await db.execute(
    select(Grade).where(
        Grade.student_id == data.student_id, Grade.course_id == data.course_id
    )
)
if r.scalar_one_or_none():
    raise HTTPException(status_code=400, detail="该学生该课程已有成绩记录")
```

校验逻辑：
- 根据请求中的 `student_id` 和 `course_id` 查询成绩表
- 如查询到已存在记录，则直接返回错误，不执行后续写入

## 二、校验失败时接口返回

接口使用 FastAPI 的 `HTTPException` 返回错误，具体如下：

| 校验场景 | 状态码 | 返回错误信息 | 代码位置 |
|---------|--------|------------|---------|
| 学生不存在 | 400 | `{"detail": "学生不存在"}` | `backend/app/api/grades.py:82-83` |
| 课程不存在 | 400 | `{"detail": "课程不存在"}` | `backend/app/api/grades.py:85-86` |
| 该学生该课程已有成绩 | 400 | `{"detail": "该学生该课程已有成绩记录"}` | `backend/app/api/grades.py:92-93` |
| 成绩记录不存在（更新/删除） | 404 | `{"detail": "成绩记录不存在"}` | `backend/app/api/grades.py:63-64` |
| Pydantic 参数校验失败（如分数不在 0-100） | 422 | FastAPI 自动返回详细校验错误 | `backend/app/schemas/grade.py:10` |

## 三、接口登录鉴权情况分析

### 当前状态

经全代码库检索，**代码中未找到**任何登录鉴权相关实现：
- 无用户模型、登录接口、Token 校验中间件
- 无 `auth`、`login`、`token`、`jwt`、`password` 等相关代码
- 所有 API 接口均为公开访问，无任何权限校验

### 可能带来的风险

1. **数据泄露风险**：任何人可直接调用接口查询所有学生、课程、成绩信息，敏感数据无保护
2. **数据篡改风险**：未授权用户可随意新增、修改、删除成绩记录，数据完整性无法保障
3. **操作不可追溯**：无用户身份标识，无法对操作行为进行审计追踪
4. **恶意攻击风险**：接口暴露后易被爬虫批量抓取数据，或被恶意调用进行数据破坏
5. **无权限分级**：无法区分管理员、教师、学生等不同角色的操作权限
